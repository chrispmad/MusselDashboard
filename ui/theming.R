mussel_theme = bslib::bs_theme()
mussel_theme <- bs_add_rules(mussel_theme, '.summary_number {font-size:larger;text-align:center;margin-top:-1rem;}')
mussel_theme <- bs_add_rules(mussel_theme, '.center_title {text-align:center !important;}')
